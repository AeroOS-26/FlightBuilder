import { ContentPage } from '@/components/common'

export const metadata = { title: 'Support · Perro Air' }

export default function Page() {
  return <ContentPage title="Support" intro="We are a small team and answer directly. There is no ticket queue and no chatbot." />
}
